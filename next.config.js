/** @type {import('next').NextConfig} */
const nextConfig = {
    //Descomentar para exportar la app como un sitio estático (npm run build)
    // output: 'export',
    // basePath: '/gestion-admin',
    // eslint: {
    //     ignoreDuringBuilds: true, 
    //     disable: true, 
    // },
};

module.exports = nextConfig;
